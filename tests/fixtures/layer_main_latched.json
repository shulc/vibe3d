{
  "name": "layer_main_latched",
  "description": "Frozen reference behaviour: the mesh EDIT TARGET is latched, not derived from the item selection. It tracks the last-selected MESH and is untouched by selecting a non-mesh item. So the transform set and the edit target are two different things — which is exactly how selecting only a reference-image item avoids dragging the mesh.",
  "source": "Frozen reference capture (headless, scripted): a two-mesh scene plus one non-mesh item, six selection steps, reading the edit target's identity after each.",
  "provenance": {
    "schema": 1,
    "source": "live-capture",
    "reference": "ref-editor@11.2v3",
    "method": "self-drive",
    "captured_utc": "2026-08-08",
    "harness": "private headless capture harness (case script + per-case result JSON live in the private tree)",
    "task": "0612 / 0614 side-question",
    "notes": "A ONE-mesh scene cannot answer this — the edit target would have only one possible non-empty value, so 'it never changed' would be equally consistent with latched and with constant. Two earlier runs were exactly that and are not evidence on their own; only the two-mesh run below is. An independent second line of evidence was obtained in the same session: with ONLY the non-mesh item selected, an auto-fit operation on it fitted to the UNSELECTED mesh's bounds."
  },
  "rig": {
    "meshes": 2,
    "non_mesh_items": 1,
    "labels": {
      "0": "mesh A",
      "1": "mesh B",
      "2": "non-mesh item"
    }
  },
  "rows": [
    {
      "step": 1,
      "item_selection": [
        "mesh A"
      ],
      "edit_target": "mesh A",
      "set_asserted": true,
      "role": ""
    },
    {
      "step": 2,
      "item_selection": [
        "mesh B"
      ],
      "edit_target": "mesh B",
      "set_asserted": true,
      "role": "CONTROL — the edit target DID move when a different mesh was selected, so the read is live. Without this row every later 'it did not move' is worthless: an inert read predicts the same thing."
    },
    {
      "step": 3,
      "item_selection": [
        "non-mesh item"
      ],
      "edit_target": "mesh B",
      "set_asserted": true,
      "role": "latched — selecting a non-mesh item leaves the edit target where the last mesh selection put it",
      "set_note": "TASK 0668: the reference's set — the non-mesh item ALONE — is representable now, so this row asserts it. It is the parity 0668 bought, and it is what keeps the divergence row below non-vacuous: a fixture whose every assertion is 'we differ' passes just as well when the channel is broken.",
      "divergence": "edit_target_unlatches_on_nonmesh_exclusive_select",
      "vibe3d_measured_edit_target": -1
    },
    {
      "step": 4,
      "item_selection": [
        "mesh A"
      ],
      "edit_target": "mesh A",
      "set_asserted": true,
      "role": "re-arm, so step 5 latches onto a DIFFERENT mesh than step 3 did"
    },
    {
      "step": 5,
      "item_selection": [
        "non-mesh item"
      ],
      "edit_target": "mesh A",
      "set_asserted": true,
      "role": "latched again, and to the other mesh — so the value follows the last mesh selection rather than being pinned to one particular layer",
      "set_note": "TASK 0668: the reference's set — the non-mesh item ALONE — is representable now, so this row asserts it. It is the parity 0668 bought, and it is what keeps the divergence row below non-vacuous: a fixture whose every assertion is 'we differ' passes just as well when the channel is broken.",
      "divergence": "edit_target_unlatches_on_nonmesh_exclusive_select",
      "vibe3d_measured_edit_target": -1
    },
    {
      "step": 6,
      "item_selection": [
        "mesh A",
        "non-mesh item"
      ],
      "edit_target": "mesh A",
      "set_asserted": true,
      "role": "a mixed selection resolves to the mesh. The ONLY row that exercises an ADD (every other row is an exclusive set), and the only one whose selected SET the reference and vibe3d agree on — so it is where an `add` that no-ops on a non-mesh item, or that evicts the mesh already in the set, is caught."
    }
  ],
  "control_row_is_load_bearing": "Step 2 is why this fixture asserts anything. Every other row reports that the edit target did NOT follow the selection; a broken or constant read reports that too. Step 2 is the only row where the two predictions differ.",
  "step_6_is_not_a_repeat": "Step 6's edit-target column alone cannot fail: it opens with `set mesh A`, which already forces the edit target to mesh A, so an `add` that does nothing reads the right answer. What makes the row discriminating is the pair of columns beside it — the item-selection FOCUS must have moved onto the added non-mesh item, and the selected SET must be exactly the two items named. Steps 1, 2 and 4 cannot substitute: they never issue an `add` at all, and steps 3 and 5 reach the non-mesh item through an exclusive `set`, a different path.",
  "corroboration": {
    "what": "auto-fit on the non-mesh item, with only that item selected",
    "result": "it fitted to the UNSELECTED mesh's bounds",
    "detail": "the item went from unit scale to (3,3,1) and from the origin to (5,0,1.5), matching the mesh that was sitting at position (5,0,0) with scale (3,3,3)",
    "why_it_corroborates": "a second, independent consumer of the edit target reached the same latched mesh"
  },
  "not_answered": {
    "what": "whether a non-mesh item is pickable in the viewport",
    "why": "The item's image link defaults to none, so it draws nothing and a click cannot reach it. The control leg — forcing it selectable — selected nothing on a centre click, which proves the rig could not deliver a click to it at all; the default leg's 'nothing was selected' would therefore have been vacuous. Blocked, not negative."
  },
  "classification": {
    "verdict": "mixed",
    "was": "Until task 0668 this fixture's verdict was \"match\": vibe3d reproduced the edit-target column row for row, and the only difference was the selected SET on steps 3 and 5, which vibe3d could not hold. 0668 TRADED those two halves. It made the exclusive select exclusive for every kind, so the reference's set is now reproducible — and it did so by dropping the edit target, which the reference keeps. Neither state is full parity; only the latched-target model (model M) is.",
    "divergences": {
      "edit_target_unlatches_on_nonmesh_exclusive_select": {
        "status": "open",
        "rows": [
          3,
          5
        ],
        "reference": "the edit target stays LATCHED on the last-selected mesh",
        "vibe3d": "there is no edit target at all (`active` reports -1)",
        "why": "The reference's edit target is a latched pointer that is NOT a member of the item selection, so an exclusive select of a non-mesh item empties the selection without touching it. vibe3d derives foreground from the selection (`foreground(l) == visible && selected`), so a latched-but-unselected edit target would draw as BACKGROUND — dimmed and read-only — while the toolpipe wrote to it. Task 0654 examined that state and declared it unrepresentable, so vibe3d has two choices and the reference's is not among them: keep the mesh SELECTED (pre-0668, which is the bug the owner reported) or drop the target (0668).",
        "closed_by": "model M — separating the edit target from the item selection throughout the draw derivation, the panels, `/api/layers` and `.v3d`. Named as its own task in `Document.itemTransformTargets`' doc comment.",
        "corroboration_still_stands": "The auto-fit reading recorded under `corroboration` is evidence for the REFERENCE's latched law, not for ours. It is unaffected by this marker and is the thing model M would have to reproduce."
      }
    },
    "live_parity_rows": [
      1,
      2,
      4,
      6
    ],
    "live_parity_note": "Rows 1, 2, 4 and 6 are asserted against the reference with no marker, including the CONTROL (row 2) and the only `add` row (row 6). They are what demonstrates the channel can still AGREE, which is what stops the two divergence rows being vacuous."
  }
}
