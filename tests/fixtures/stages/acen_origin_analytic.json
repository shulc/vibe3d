{
  "schema": "stage_conformance.v1",
  "stage": "acen",
  "name": "acen_origin_analytic",
  "description": "ACEN stage-conformance, Mode.Origin (task 0342 Phase 3): trivial constant world (0,0,0), independent of mesh/selection state (source/toolpipe/stages/actcenter.d computeCenter's Mode.Origin arm returns Vec3(0,0,0) unconditionally). Two cases pin the SAME golden under different selections to demonstrate the selection-independence explicitly rather than assuming it from a single case.",
  "source": "Analytic -- Mode.Origin is a hard-coded constant in the stage under test, no reference measurement needed (no formula to derive). Cross-checked against a live vibe3d run at authoring time.",
  "provenance": {
    "schema": 1,
    "source": "analytic",
    "reference": "analytic",
    "method": "hand",
    "captured_utc": "unknown",
    "harness": "hard-coded constant in the stage under test",
    "task": null,
    "notes": "Mode.Origin is a hard-coded constant; no formula to derive, no reference measurement needed."
  },
  "parameters": {
    "schema": 1,
    "state": "recorded",
    "derived_from": "replay-steps",
    "cells": [
      {
        "cell": "origin_no_selection",
        "drove": [
          {
            "op": "actr.origin",
            "values": {}
          }
        ]
      },
      {
        "cell": "origin_with_offcenter_selection",
        "drove": [
          {
            "op": "actr.origin",
            "values": {}
          }
        ]
      }
    ],
    "notes": "drive record read from tests/fixtures/stages/acen_origin_analytic.json:22; Same shape as acen_local_analytic: each case's only non-stand step is the bare action-centre mode command in `pipe_setup` (lines 22 and 32), which takes no arguments. `reset` and the second case's vertex `selection` are stand — and the second case exists precisely to show the answer is selection-independent. THE JUDGMENT: the golden is a hard-coded constant in the stage under test, so nothing was captured; the drive record is the fixture's own replay."
  },
  "tolerance": 0.0001,
  "cases": [
    {
      "name": "origin_no_selection",
      "mesh_build": [ { "reset": true } ],
      "pipe_setup": [ "actr.origin" ],
      "golden": { "center": [0.0, 0.0, 0.0] }
    },
    {
      "name": "origin_with_offcenter_selection",
      "mesh_build": [ { "reset": true } ],
      "selection": {
        "mode": "vertices",
        "coords": [ [0.5, 0.5, 0.5] ]
      },
      "pipe_setup": [ "actr.origin" ],
      "golden": { "center": [0.0, 0.0, 0.0] }
    }
  ]
}
