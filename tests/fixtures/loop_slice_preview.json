{
  "name": "loop_slice_preview",
  "description": "Loop Slice preview-state parity: transient tool state (count / mode / slice positions) read from /api/tool/state after activating on a seed edge.",
  "source": "Self-golden capture via gen.py selfdrive-loopslice.",
  "provenance": {
    "schema": 1,
    "source": "simulated",
    "reference": "vibe3d-selfgen",
    "method": "self-drive",
    "captured_utc": "unknown",
    "harness": "tools/local/fixture_gen/gen.py selfdrive-loopslice",
    "task": "0343",
    "notes": "preview-state fixture (belt_count3): transient tool state read from vibe3d's own /api/tool/state, not reference-app parity."
  },
  "tolerance": 0.0001,
  "cases": [
    {
      "name": "belt_count3",
      "input": [
        {
          "reset": true
        },
        {
          "select": {
            "mode": "edges",
            "coords": [
              [
                [
                  -0.5,
                  -0.5,
                  -0.5
                ],
                [
                  0.5,
                  -0.5,
                  -0.5
                ]
              ]
            ]
          }
        },
        {
          "endpoint": "command",
          "argstring": "tool.set mesh.loopSliceTool on"
        },
        {
          "endpoint": "command",
          "argstring": "tool.attr mesh.loopSliceTool count 3"
        }
      ],
      "state_checks": {
        "count": 3,
        "mode": "uniform",
        "positions": [
          0.25,
          0.5,
          0.75
        ]
      }
    }
  ]
}
