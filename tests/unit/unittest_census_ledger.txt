# Declared drops in the test census — the ledger the gate reads (task 0835).
#
# The gate lives in tests/unit/unittest_census_gate.d and runs inside
# `dub test --config=tests`. It counts `unittest` blocks and assertion tokens
# over source/ ∪ tests/unit/ in the working tree, counts them again at every
# revision of the current lane (the branch point and each commit since) with
# the same scanner, and asserts:
#
#     live count + declared drops  never falls below the highest this lane
#     has already reached
#
# Growth needs nothing from you: add tests and the gate stays green with no
# file to edit and no number to bump — committing them just raises the bar.
# Only a DROP needs a line here.
#
# FORMAT — one line per declared drop, appended at the end:
#
#     drop <blocks> <asserts> <task-id> <what was removed, and why it was right>
#
# Both numbers are MAGNITUDES (how many were lost), not signed deltas. The
# arithmetic has to close exactly: the gate prints the shortfall it still sees,
# so declaring 1 block when 40 went missing leaves the lane red and names the
# files that lost them. Over-declaring is arithmetically possible and is meant
# to be conspicuous — a line reading `drop 279 1492` is the point.
#
# Lines already present at the branch point are already folded into the
# baseline, so they cannot be spent twice: only what a lane ADDS here grants
# that lane an allowance. What accumulates instead is the useful thing — an
# auditable record of every test this project has ever deliberately given up.
#
# WORKED EXAMPLE (task 0727, which landed before this gate existed; commented
# out so it grants nothing, and reproduced by the scanner to the digit):
#
#     drop 1 9 0727 the unittest of the deleted class-blind LIFO stepping path
#
# WHAT DOES NOT BELONG HERE. "The extraction lost some blocks, unclear which"
# is not a declaration, it is the 0706 incident with a number attached. If you
# cannot say what was removed, the answer is to find it, not to declare it.

# tests/unit/visibility_cache_test.d deleted along with the VisibilityCache
# wrapper it tested (source/visibility_cache.d): the wrapper had no live
# caller (the lasso path it served moved to gpuSelect.elementVisibility).
# Mesh.visibleVertices, the one thing it wrapped, stays with its own tests
# and live callers (snap, the facing predicate).
drop 1 7 0861 deleted the VisibilityCache wrapper (source/visibility_cache.d) and its test — no live caller

# mesh_ops/loop_slice.d, insertEdgeLoopsMulti (task 1902 Stage C): removed
# the length-only asserts on newMat/newPart ("newMat.length == faces.length
# && newPart.length == faces.length") and newSetMask ("newSetMask.length ==
# faces.length") — those arrays no longer exist. What replaced them is NOT
# a plane-length assert inside mesh_planes.rewriteFaces (it asserts only
# `FaceSource.oldOfNew.length == newFaces.length`, i.e. newSrc against
# newFaces — do not go hunting there for a faceMaterial/facePart/faceSetMask
# length check, it does not exist); it is the by-construction
# `built.length = newFaces.length` rewriteFaces sets for every plane in
# kFacePlanes before filling it from `newSrc`, so each carried plane can
# never end up a different length than newFaces. The mutation drill (drop
# faceMaterial's carry right after the rewriteFaces call) still reddens
# tests/unit/mesh_ops/loop_slice_test.d's existing value assertion, so the
# invariant these two lines checked is still covered — the check moved, it
# was not lost.
drop 0 2 1902 mesh_ops/loop_slice.d's newMat/newPart/newSetMask length asserts are gone with the arrays themselves — mesh_planes.rewriteFaces asserts the same invariant once, internally

# app.d, task 0781 step 2d: the `static assert` that kept step 2c's
# `gestureTrack` forwarder BY REF (`is(typeof(&gestureTrack()) ==
# GestureTrack*)`) is gone with the forwarder itself. It guarded exactly one
# hazard — a `@property` forwarder that dropped `ref` would have let
# `gestureTrack.event(...)` advance a temporary COPY, silently — and that
# hazard has no subject any more: `handleMouseButtonDown`/`Up`, the two
# main()-side readers the forwarder existed for, are now InputRouter methods
# naming the router's OWN `GestureTrack` field bare. There is no forwarder
# left to get wrong, so this is a guard whose object was deleted, not a check
# that was weakened. The OTHER assert app.d lost in the same commit is a pure
# MOVE and is not declared here: `assert(gpuUploadedPreview, "lasso: preview
# trace is live but the VBOs still hold the cage")` travelled verbatim (as
# `app.gpuUploadedPreview`) into input_router.d's `handleMouseButtonUp`, which
# is why the net is -1 and not -2.
drop 0 1 0781 the `static assert` guarding step 2c's `gestureTrack` @property-ref forwarder — the forwarder itself is gone with its last main()-side readers

# The two tracker-on / tracker-off PARITY cells (W-d3c in
# tests/unit/commands/mesh/position_delta_test.d, W-b4 in
# tests/unit/commands/mesh/symmetry_delta_test.d). Each ran its commands twice
# in one process — once with VIBE3D_UNDO_TRACKER on, once off — and asserted
# the two post-revert plane dumps agreed. Their whole value was that the OFF
# arm was an INDEPENDENT data path: the command's own stored pre-op arrays
# rather than the op-log's `posBefore`. Task 1903 stage N deleted the hatch, so
# that arm does not exist; the cells would have set the flag to the value it
# already holds and compared a path against itself, which is green before a
# defect and green after it. Removed rather than left standing, and the
# observable they carried is still read: the FROZEN oracle
# tests/fixtures/undo_parity/position_marks.json (undo_parity_l0_test.d), which
# was captured on the snapshot arm before that arm was deleted and now has no
# producer at all, plus W-b5's FORWARD replay, which is the cell that pins the
# ORDER of mesh.transform's two entries.
drop 2 16 1903 the two tracker-on/off parity cells whose OFF arm was the undo hatch — the hatch is gone, the frozen parity oracle and W-b5's forward replay still see what they saw

# NOT A DROP LINE — the arithmetic already closed, and this is the audit trail
# the gate could not ask for. Task 1903 stage N deleted `withHatch` and its
# witness block from tests/unit/undo_parity_l3_test.d: the helper flipped the
# `VIBE3D_UNDO_TRACKER` module global under a `scope (exit)` because druntime
# runs every unittest module in one process, and the block proved the restore
# happened on the normal path AND on a throw. The flag is gone, so the hazard
# has no subject. The same commit added tests/unit/undo_hatch_removed_census_test.d,
# whose two blocks more than cover the loss, which is why no `drop` line is
# owed here — recorded so the removal is not invisible to a later reader.
drop 2 9 3691 removed obsolete synthetic pre-cutover PreparedArm API tests after production unified transaction replacement

# Task 4066 row 8 was written, committed, and then REVERTED inside its own lane:
# the edit deleted two strings that tools/check_prepared_protocol.py pins
# verbatim (`macroRecorder.bindObserverHub(recordObserverHub);` in source/app.d,
# `void bindObserverHub(RecordObserverHub hub)` in source/macro_recorder.d),
# moved a reference count the same scanner pins (production_hub_refs 7 -> 8),
# and drifted the evolvePrepared digest plus its trace-cap mutation anchor. The
# one assert lost here is the `assert(hub !is null)` that row 8's constructor
# introduced; reverting the row takes it back out, so source/macro_recorder.d
# returns to exactly its state on main. Nothing that existed before this lane is
# gone. The row itself is carried to backlog card 4072, blocked on the same
# P1.0c cutover decision that refuted row 4.
drop 0 1 4066 the assert(hub !is null) added by row 8's MacroRecorder constructor, removed with the row when it was reverted for breaking five pins of the prepared-protocol census

# Task 4062 deleted `oneStringArg` from source/http_providers.d — the helper
# that read the single string argument of five `viewport.*` commands, and the
# unittest that pinned its three accepted spellings plus the positional-wins
# precedence. The function is gone because those five commands DECLARE that
# argument now (`params()` is the positional order), and every spelling it
# accepted is pinned again in tests/unit/command_bindargs_test.d and
# tests/unit/command_bindargs_parity_test.d — the bare-scalar body, the
# `_positional` array, the named key, and positional-wins-over-named all have
# rows there, over the real commands rather than over a private helper. Its two
# NON-answers are the exception and are not re-pinned: it read a numeric
# positional as "" (no argument at all), and under the one coercion law a
# number reaching a String slot is spelled out — the command hears the argument
# and refuses it for its own reason instead of silently keeping its default.
# Eight of its asserts went with it; the net loss after the two new files is
# three.
drop 0 3 4062 the oneStringArg unittest in source/http_providers.d, deleted with the helper when the viewport commands began declaring their own argument; every spelling it pinned is re-pinned over the real commands in tests/unit/command_bindargs_{,parity_}test.d

# TASK 4100 (review round). `commit_seam_census_test.d`'s §2.6 widening block
# used to name each row's declaring file as a literal path — a `"source/mesh.d"`
# default on the row struct plus three `"source/mesh_edge_slice.d"` overrides —
# which is the move-fragility the rest of this task removed everywhere else.
# The file is now DERIVED from the row's own `publicDecl`, so three asserts go
# (`exists(meshPath)`, `exists(esPath)`, and `w.declFile in declSrc`, which only
# ever asked whether the table agreed with itself) and two arrive in their place
# (`declFilesScanned >= 100`, the walk's non-vacuity floor, and `seen.length == 1`,
# which reddens both when the declaration is gone and when the signature is
# realised in two modules). Net -1 assert, and the coverage is strictly wider:
# the old lookup could not see a deleted declaration at all.
drop 0 1 4100 commit_seam widening block derives its declaring file from publicDecl instead of naming it as a path; three table-consistency/exists asserts replaced by two tree-facing ones

# Task 4053 — the DROP conversion was landed, measured, and reverted inside the
# same task, so three asserts that only had a subject while it stood go with it.
# The three: `PreparedDrop`'s non-copyable `static assert` in
# tests/unit/prepared_tool_transition_test.d and the matching `static assert`
# on `commitPreparedDrop`'s nothrow function type in
# source/prepared_tool_transition.d, both of which pinned a type that no longer
# exists; and one row of the ownership ledger's partition count, which went
# from three doors (arm / prepared drop / legacy drop) to two, so its third
# count has nothing to count. Nothing that survived lost a check: the partition
# is still pinned in both directions (kArms, kLegacyDrops), the arm pair keeps
# its own nothrow and non-copyable asserts, and the reason for the revert is
# pinned by three DRIVEN cells that the conversion reddened —
# tests/test_rs_insession_cancel.d:517, tests/test_item_drag_undo.d:288 and
# tests/test_tool_gesture_g2.d:388.
drop 0 3 4053 PreparedDrop's non-copyable assert, commitPreparedDrop's nothrow function-type assert, and the ownership ledger's third door count — all three pinned the prepared drop transaction that this same task measured as not yet a faithful twin of Tool.deactivate() and removed again

# Task 4335 (review fix round) — one assert removed from
# tests/unit/mesh_ops/edge_bevel_test.d because it could not come out
# differently. It read `faces.length >= facesBefore + 2` and sat two lines
# under an EXACT `wantF` pin of 6 at L0 and 7 at L1, on a fixture whose
# `facesBefore` is 4 — so it was green whatever the code did, and would have
# stayed green through the very refusal the block exists to catch. Nothing it
# was reaching for went with it: the +2/+3 reading is still pinned, by the
# exact `wantF` assert above it plus the premise block's `d == 4`, and every
# face of that half-disk touches the hub. No replacement `facesBefore == 4`
# was added, because that would be the same defect one step along.
drop 0 1 4335 the open-fan cap block's `faces.length >= facesBefore + 2`, which an exact wantF pin two lines above already fixed — a check that could not come out differently
