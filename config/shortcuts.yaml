tools:
  move:   W
  rotate: E
  scale:  R
  box:    ""
  bevel:  B

commands:
  select.expand:         "Shift+Up"
  select.contract:       "Shift+Down"
  select.more:           Up
  select.less:           Down
  select.loop:           L
  select.ring:           "Alt+L"
  select.invert:         "["
  select.connect:        "]"
  select.between:        "Shift+G"
  viewport.fit:          A
  viewport.fit_selected: "Shift+A"
  mesh.subdivide:        "Shift+D"
  history.undo:          "Ctrl+Z"
  history.redo:          "Ctrl+Shift+Z"

editmodes:
  vertices: "1"
  edges:    "2"
  polygons: "3"
